services:
  - cluster
  - session
  - storage
  - meta
  - task
cluster:
  backend: fixed
  node_timeout: 120
  node_check_interval: 1
storage:
  backends: [plasma]
  plasma:
    store_memory: 20M
meta:
  store: dict
