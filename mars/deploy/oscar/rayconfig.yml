services:
  - cluster
  - session
  - storage
  - meta
  - lifecycle
  - task
  - web
cluster:
  backend: fixed
  node_timeout: 120
  node_check_interval: 1
  ray:
    supervisor:
      standalone: no
      sub_pool_num: 0
storage:
  backends: [ray]
meta:
  store: dict
task:
  default_config:
    optimize_tileable_graph: yes
    optimize_chunk_graph: yes
    fuse_enabled: yes
