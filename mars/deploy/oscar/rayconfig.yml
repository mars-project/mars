"@inherits": base_config.yml
cluster:
  backend: ray
  ray:
    supervisor:
      standalone: no
      sub_pool_num: 0
session:
  custom_log_dir: null
storage:
  backends: [ray]
scheduling:
  autoscale:
    enabled: true
    migrate_data: false
    scheduler_backlog_timeout: 10
